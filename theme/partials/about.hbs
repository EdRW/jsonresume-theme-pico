{{#resume.basics}}
<section>
  <h2>About</h2>
  <address>
    {{! contact info }}

    {{#if location}}
    <div>
      Location:
      {{ location.city }}
      {{ location.countryCode }}
    </div>
    {{/if}}
    {{#if email}}
    <div>
      Email: <a href="mailto:{{ email }}">{{ email }}</a>
    </div>
    {{/if}}
    {{#if phone}}
    <div>
      Phone: <a href="tel:{{ phone }}">{{ phone }}</a>
    </div>
    {{/if}}
    {{#if url}}
    <div>
      Website: <a href="{{ url }}">{{ url }}</a>
    </div>
    {{/if}}
  </address>

  {{! social links }}
  {{#if profiles.length}}
  <div>
    {{#each profiles}}
    <div>
      <span class="fa fa-{{ network }}">{{ network }}: </span>
      {{#if url}}
      <a href="{{ url }}" target="_blank">
        {{#if username}}
        ({{ username }})
        {{else}}

        {{ url }}
        {{/if}}
      </a>
      {{/if}}
    </div>
    {{/each}}
  </div>
  {{/if}}
</section>
{{/resume.basics}}
